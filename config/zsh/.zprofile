# .zprofile is sourced on login shells, before .zshrc.
# Keep it minimal — just environment setup that must happen at login.

# Nothing macOS-specific here. Add login-time env exports below if needed.
